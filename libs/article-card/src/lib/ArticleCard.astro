---
interface Props {
  post: {
    title: string
    shareCard: string
    description: string
    tags: Array<{
      slug: string
      title: string
    }>
  }
}

const { post } = Astro.props
---
<article class="card bg-base-100 shadow-xl">
  {post.shareCard && (
    <figure><img src={post.shareCard} alt="Shoes" /></figure>
  )}
  <div class="card-body">
    <h2 class="card-title text-inherit">
      {post.title}
    </h2>
    <p class="text-inherit">{post.description}</p>
    {post.tags.length ? (
      <div class="card-actions justify-end">
        <span>This article is about:</span>
        {post.tags.map(tag => (
          <a href={tag.slug} class="badge badge-outline">{tag.title}</a>
        ))}
      </div>
    ) : null}
  </div>
</article>
