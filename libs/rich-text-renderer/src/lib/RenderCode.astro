---
import { Code } from 'astro/components';

const { code, lang = 'js', theme = 'monokai' } = Astro.props;
---

<Code code={code} lang={lang} theme={theme} />

<style is:global>
  pre {
    overflow-y: auto;
    padding: 1.2rem;
  }
</style>
