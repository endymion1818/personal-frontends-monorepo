---

interface Props {
  menuItems: Array<{
    title: string,
    link: string
  }>;
  logo?: string;
  title: string;
}

const {menuItems, logo, title } = Astro.props
---
<header class="navbar bg-base-100 rounded-md mt-2">
  <div class="flex-none">
    <a class="btn btn-ghost normal-case text-xl hover:bg-white">
      {logo ? <img src={logo}> : <span>{title || null}</span>}
    </a>
  </div>
  <div class="flex-1 items-end">
    {menuItems?.length ? (
      <ul>
        {menuItems.map(item => (
          <li class="text-base"><a class="btn btn-ghost" href={item.link}>{item.title}</a></li>
        ))}
      </ul>
    ): null }
  </div>
</header>
